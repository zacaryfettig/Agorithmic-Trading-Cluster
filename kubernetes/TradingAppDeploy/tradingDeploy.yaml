apiVersion: apps/v1
kind: Deployment
metadata:
  name: trading-app
spec:
  replicas: 1
  selector:
    matchLabels:
      app: trading-app
  template:
    metadata:
      labels:
        app: trading-app
    spec:
      containers:
        - name: trading-app
          image: ${TRADING_APP_IMAGE}
          imagePullPolicy: Always
          env:
            - name: APCA_API_KEY
              valueFrom:
                secretKeyRef:
                  name: alpaca-secrets
                  key: APCA_API_KEY

            - name: APCA_API_SECRET
              valueFrom:
                secretKeyRef:
                  name: alpaca-secrets
                  key: APCA_API_SECRET

            - name: APCA_API_BASE_URL
              valueFrom:
                secretKeyRef:
                  name: alpaca-secrets
                  key: APCA_BASE_URL